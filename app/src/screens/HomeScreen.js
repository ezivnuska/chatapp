import React from 'react'
import {
    Text,
    TouchableOpacity,
    View,
} from 'react-native'
import { SigninScreen } from './'

const HomeScreen = props => (
    <View>
        <SigninScreen />
    </View>
)

export default HomeScreen